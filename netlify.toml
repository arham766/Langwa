[build]
  command = "npm run build"
  publish = "dist"  # Ensure this matches your output folder

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.processing]
  skip_processing = true  # Optional, if post-processing is causing issues
